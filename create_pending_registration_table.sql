CREATE TABLE IF NOT EXISTS "PendingRegistration" (id TEXT NOT NULL, name TEXT NOT NULL, email TEXT NOT NULL, identifier TEXT NOT NULL, passwordHash TEXT NOT NULL, otpHash TEXT NOT NULL, expiresAt TIMESTAMP(3) NOT NULL, attempts INTEGER NOT NULL DEFAULT 0, lastSentAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, createdAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "PendingRegistration_pkey" PRIMARY KEY (id)); CREATE UNIQUE INDEX IF NOT EXISTS "PendingRegistration_email_key" ON "PendingRegistration"("email");
